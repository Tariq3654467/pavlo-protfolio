# Suppress deprecation warnings for legacy dependencies
legacy-peer-deps=true

# Optional: suppress funding messages
fund=false

# Optional: suppress audit warnings for optional dependencies
audit=false

